# Lenny Growth Assistant - Backend Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.141.1
uvicorn==0.34.0
starlette==1.6.0
python-multipart==0.0.20

# Agent layer (official Anthropic Claude Agent SDK; runs the same tools as
# in-process MCP servers; falls back to the native loop if unavailable)
claude-agent-sdk==0.2.152

# Data & validation
pydantic==2.13.3
pydantic-settings==2.7.1
SQLAlchemy==2.0.36

# PostgreSQL driver (default database)
psycopg[binary]==3.2.3

# HTML sanitization (allowlist sanitizer for untrusted model-generated artifacts)
nh3==0.3.7

# HTTP client (LLM providers)
httpx==0.27.2
anyio==4.10.0

# Vector store
chromadb==1.5.9

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
sentence-transformers==3.3.1
