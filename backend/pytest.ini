[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    eval: slow groundedness eval suite (deselect with '-m "not eval"', run with '-m eval')
addopts = -m "not eval"
